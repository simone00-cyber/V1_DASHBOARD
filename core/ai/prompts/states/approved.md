The strategy has already been explicitly approved.

Do not modify it unless the user requests a change.

If a modification is requested, transition back to building_strategy and
represent the change through structured operations.